/**
 * SOP Configuration File
 * Update this file at the start of every 4-month cycle to pre-load 
 * the dropdowns with standard Epics and User Stories.
 */

window.SOP_CONFIG = {
  // Add predefined Epics here
  epics: [
    // Example: "EPIC-100: BASE SETUP",
  ],
  
  // Add predefined User Stories here
  userStories: [
    // Example: { key: "WF-1001: LOGIN", epic: "EPIC-100: BASE SETUP" },
  ]
};
